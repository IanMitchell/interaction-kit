name: Formatting

on: [push, pull_request]

jobs:
  Setup:
    uses: ianmitchell/interaction-kit/.github/workflows/setup.yml@1b7470aa499e9ce44ee4882ee6836da7f1f146e3

  Prettier:
    runs-on: ubuntu-latest
    needs: Setup
    steps:
      - uses: actions/checkout@v2
      - name: Formatting
        run: |
          yarn prettier

{
	"name": "{{name}}",
	"main": "src/app.ts",
	"type": "module",
	"scripts": {
		"deploy": "NODE_OPTIONS='--es-module-specifier-resolution=node' ikit deploy",
		"dev": "NODE_OPTIONS='--es-module-specifier-resolution=node' ikit dev"
	},
	"engines": {
		"node": ">=16"
	},
	"dependencies": {
		"interaction-kit": "^0.1.0",
		"fastify": "^2.0.0",
		"dotenv": "^10.0.0"
	}
}

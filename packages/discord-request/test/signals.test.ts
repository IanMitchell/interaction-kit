import { test } from "vitest";

test.todo("Abort signal interrupts queue sweeps");

test.todo("Abort signals interrupt in-flight requests");

test.todo("Abort signals stops global delay timer");

test.todo("Abort shuts down discord-request");

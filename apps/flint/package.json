{
	"name": "flint-bot",
	"main": "./src/app.ts",
	"type": "module",
	"scripts": {
		"clean": "rimraf .turbo .ikit",
		"deploy": "ikit deploy",
		"bot": "DEBUG=* ikit dev",
		"typecheck": "tsc --noEmit",
		"prettier": "prettier src/**/*.ts --check",
		"lint": "eslint ./src/**/*.ts"
	},
	"engines": {
		"node": ">=18"
	},
	"platform": "node",
	"dependencies": {
		"interaction-kit": "0.2.2",
		"esbuild": "0.15.13"
	}
}
